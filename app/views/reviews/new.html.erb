<div class="container mt-5">
  <h1>New review</h1>

  <%= simple_form_for [@restaurant, @review] do |f| %>
    <%= f.input :content, as: :text, label: "Your review" %>

    <%= f.input :note, 
                as: :integer, 
                label: "Rating (0 à 5)", 
                input_html: { min: 0, max: 5, step: 1 } %>

    <%= f.button :submit, "Submit", class: "btn btn-primary" %>
  <% end %>
</div>
